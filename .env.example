# Session cookie signing (optional in dev: app falls back to "dev-secret").
# In production, set SESSION_SECRET in your host's environment.
# SESSION_SECRET=your-secret-here

# Docker Compose
# Optional override for the Redis URL used by services when running via docker-compose.
# Default is redis://redis:6379, which matches the compose file.
# REDIS_URL=redis://redis:6379
